import React from "react";
import StartFragment from "./fragments/StartFragment";

const Form: React.FC = () => {
    return (
        <div>
            <StartFragment />
        </div>
    );
};

export default Form;